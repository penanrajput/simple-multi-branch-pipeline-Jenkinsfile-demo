pipeline {
  agent any
  stages {
    stage('java stage') {
      steps {
			echo 'Hello, JDK'
			// sh 'java -version'
      }
    }
    stage('maven stage') {
      steps {
			echo 'Hello, Maven'
			// sh 'mvn -version'
      }
    }
  }
}