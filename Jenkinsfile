pipeline {
    agent any
    parameters {
        string(name: 'learning', city:'jalgaon' )
    }
    stages {
        stage('test') {
            steps {
                echo '${params. name } is the parameter'
            }
        }
    }
}
